/**
 * Deploy-time student profile.
 *
 * The values below are placeholders and are the only thing this public repository
 * ever contains. The GitHub Pages workflow substitutes them from repository
 * secrets when it builds the site, so the child's name never enters the source or
 * its git history. Any field left as a placeholder is simply ignored.
 *
 * Nothing here applies when the backend is running: a real sync reads the profile
 * from the portal instead.
 */
window.VIBGYOR_STUDENT = {
  name: "__STUDENT_NAME__",
  grade: "__STUDENT_GRADE__",
  section: "__STUDENT_SECTION__",
  school: "__STUDENT_SCHOOL__",
  academic_year: "__STUDENT_ACADEMIC_YEAR__",
};
